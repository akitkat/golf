---
import GolfCourseInfoItem from "./GolfCourseInfoItem.astro";
const { ids, keywords } = Astro.props;
---

{
  ids.split(',').map((id: number) => {
    return (
      <GolfCourseInfoItem
        id={id}
        keywords={keywords}
      />
    )
  })
}
